# Exact Autodesk Report 页面改造记录

> 日期：2026-08-13
> 范围：将原有的单页 `Exact_autodesk_report.html` 重构为 **User Report / Token Report** 两个标签页，并新增 Procurement 采购记录模块与配套本地服务。

---

## 1. 页面整体结构

### 1.1 从单页 → 双标签页

原 HTML 只有用户使用量报告；本次改造通过 `.tabs` 标签栏拆分为两个独立页面：

- **User Report**：原有用户使用报告（Flagged Users / User Details / 日期切换）
- **Token Report**：新增的 Token 使用汇报页

```html
<div class="tabs">
  <button class="tab active" id="tab-user" onclick="switchTab('user')">User Report</button>
  <button class="tab" id="tab-token" onclick="switchTab('token')">Token Report</button>
</div>
<div class="tab-content active" id="page-user">…</div>
<div class="tab-content" id="page-token">…</div>
```

### 1.2 停留页面记忆（localStorage）

- 切页时写入 `localStorage.setItem('autodesk_active_tab', name)`
- 页面加载时读取并 `switchTab(saved)`，**刷新后停留在上次所在页面**，不再固定回 User Report

---

## 2. Token Report 页面

### 2.1 数据来源：改为实时读取 CSV

- 不再内嵌硬编码数据，改为 `fetch('Token_2026-08-13.csv?t=' + Date.now())`，带 `cache:'no-store'`
- 每次切入 Token 页都会**重新 fetch 最新 CSV**，CSV 更新后刷新即可生效

### 2.2 Token 汇总卡片

顶部卡片：`Token Records / Users / Products / Tokens Used / Token Remains`

- **Tokens Used** 与 **Token Remains** 卡片按占比铺**半透明背景填充**：
  - Used = 蓝色 `rgba(37,99,235,.14)`，Remains = 绿色 `rgba(22,163,74,.14)`
  - 百分比 = 相对 Procurement 采购总量（`tokens / 采购总和 × 100`）
  - 百分比以小字显示在数值旁（如 `24 24.0%`），不额外占行高

### 2.3 Token Highlights 产品卡片

- 每产品一张卡：`N records · N days used` + 加粗的 `N tokens used`
- **单击**：展开 Token Details 并筛选该产品（高亮选中框）
- **再单击**：收起详情，并弹出**产品用户弹窗**（modal），列出该产品全部用户
- 弹窗内每行显示：
  - 使用天数 `N days`
  - 月均频率 `N.N/mo`（满 3 个月用 CSV 原值；**不满 3 个月按 assign date → CSV 报告日期重算** `daysUsed ÷ 实际月数`）
  - 距上次使用 `N d`（报告日期 − last_accessed）

### 2.4 Token Details 明细表

列：`Name / Email / Product / Days Used / Monthly Avg / Tokens / Last Access / Assign Date`

- 与 User Report 一致的**可排序表头 + 每列筛选行**（文本下拉 / 数字比较符 / 日期 before·after）
- **Monthly Avg 对齐 User Report 规则**：assign < 3 个月时显示重算值 `X.X (原值) i`，Assign Date 加红标
- Tokens 列带占比条（单条 / 总量）
- 合计行 `Total (N records)`

---

## 3. Procurement 采购记录模块

Token Details 下方新增 **Procurement Records** 折叠区。

### 3.1 字段

- 手动录入表格，列：**Tokens / Purchase Date / Expiry Date / Notes**（曾经历 Product→移除、Seats→移除的调整）
- **Expiry 自动填充**：填入 Purchase Date 后，若 Expiry 为空则自动补后一年（`purchase + 1 年`）
- 输入框无边框（悬浮浅边、聚焦蓝边）

### 3.2 持久化

- 弃用 localStorage，改为经本地服务写入 **`output/Procurement Records.json`**
- 每次刷新页面通过 `GET /api/procurement` 读取全部记录

### 3.3 交互

- **Add Record**：追加一行并保存
- **删除（行内 ✕ 或 Clear All）**：需输入 `delete` 确认
- 汇总卡：`Records / Tokens Purchased`

### 3.4 与 Token 卡片的联动

- Procurement 采购总和作为 Token Used / Remains 百分比的分母
- `Tokens Used = CSV 汇总`, `Token Remains = 采购总量 − 已用量`

---

## 4. 配套本地服务 server.cjs

新增 `server.cjs`（Node 原生 http，无第三方依赖），取代 `python3 -m http.server`：

- 静态托管 `output/` 目录
- API：`GET /api/procurement`（读 JSON）、`POST /api/procurement`（写 JSON）
- 启动：`node server.cjs` → `http://127.0.0.1:8000/Exact_autodesk_report.html`

---

## 5. 视觉统一

- 两页配色统一为专业蓝 `#2563eb`，移除靛蓝/紫色渐变（原 `.token-bar`、`.pct-bar` 等 AI 味配色）
- 按钮、选中 Tab、高亮单元格、产品卡片、弹窗统一同色
- 保留语义色：琥珀（avg 标记）、绿色（active 徽章）
- 滚动晃动修复：`html{scrollbar-gutter:stable}` 预留滚动条空间，避免展开表格时页面左右漂移；`max-height:3000px` 保证折叠动画平滑

---

## 6. 相关文件清单

| 文件 | 说明 |
|---|---|
| `output/Exact_autodesk_report.html` | 主报告页（双标签 + Token + Procurement） |
| `output/Token_2026-08-13.csv` | Token 数据源（实时 fetch） |
| `output/Procurement Records.json` | 采购记录持久化文件 |
| `server.cjs` | 本地服务（静态托管 + Procurement API） |
| `token_test.html` | 早期本地测试页（含 Token 分区，已由正式页取代） |
